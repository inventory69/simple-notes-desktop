feat(autosave): protect against accidental deletion of empty checklist items

- Block auto-save when checklist items have empty text
- Add visual indication for empty items (dashed border, reduced opacity)
- Show "not saved" hint next to empty items
- Auto-remove empty items on blur after 200ms delay
- Update sync status to show "Unsaved (empty item)"

This prevents users from accidentally losing checklist items when they delete all text,
giving them time to either add new text or explicitly delete the item.

Files changed:
- src/components/NoteEditor.js:
  - scheduleSave(): added hasEmptyChecklistItems() check
  - new hasEmptyChecklistItems() method
  - attachChecklistListeners(): added input handler for empty visual state
  - attachChecklistListeners(): added blur handler for auto-removal
  - renderChecklistItem(): added empty class and unsaved hint
- src/styles.css: checklist-item-empty and checklist-item-unsaved-hint styles
