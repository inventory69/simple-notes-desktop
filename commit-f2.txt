feat(checklist): implement full feature parity with Android app

- Add checklistSortOption field to Rust models and markdown frontmatter
- Implement 5 sort options: MANUAL, ALPHABETICAL_ASC, ALPHABETICAL_DESC, UNCHECKED_FIRST, CHECKED_FIRST
- Add visual separator between checked and unchecked items with completion count
- Implement drag-and-drop reordering via mouse with visual feedback
- Add sort button in editor header with dropdown menu for sort selection
- Auto-switch to MANUAL sort when items are manually reordered
- Preserve F1 keyboard handlers (Enter/Backspace) in new checklist system
- Add drag handle icon (⋮) to each checklist item
- Implement real-time separator updates when items are checked/unchecked

Default sort for new checklists is UNCHECKED_FIRST to match Android behavior.

Files changed:
- src-tauri/src/models.rs: added checklistSortOption to Note and NoteMetadata
- src-tauri/src/markdown.rs: include sort option in frontmatter
- index.html: added checklist-sort-btn button
- src/components/NoteEditor.js: complete checklist rendering rewrite with:
  - sortChecklistItems() method
  - renderSeparator() method  
  - attachChecklistListeners() method
  - setupDragAndDrop() method
  - showSortMenu() method
- src/styles.css: separator, drag-handle, and sort-menu styles
